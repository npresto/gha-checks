name: pull request comment
on:
  issue_comment:
    types: [created, edited, deleted]
jobs:
  spread:
    if: ${{ github.event.issue.pull_request }}
    runs-on: [ubuntu-latest]
    steps:
      - name: say hello
      if: contains(github.event.comment.body, '!ANTS')
      run: |
        echo "I want to die"